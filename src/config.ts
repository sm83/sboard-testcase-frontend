// it sure need to be in .env, but it just uses this confing because project is basicly public.

export const showRuntimeExceptions: boolean = true;
export const showInitExceptions: boolean = true;
export const showConstructionLogs: boolean = true;
